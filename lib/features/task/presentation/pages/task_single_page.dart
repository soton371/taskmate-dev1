import 'package:flutter/material.dart';

class TaskSinglePage extends StatelessWidget {
  const TaskSinglePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("data"),
    );
  }
}
